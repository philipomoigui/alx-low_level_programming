#include <holberton.h>

/**
 * mul - Multiply two integers
 * @a: The first integer
 * @b: The second integer
 *
 * Return: Return the result of the multiplication
 */

int mul(int a, int b)
{
int result = a * b;
 return (result);
}
